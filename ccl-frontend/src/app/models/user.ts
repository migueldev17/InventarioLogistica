// Representación de un usuario en la aplicación
export interface User {
  id: number;          // ID del usuario en la base de datos
  username: string;    // Nombre de usuario
  role: string;        // Rol (ejemplo: admin, user)
}
